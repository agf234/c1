---
platform: linux

image_resource:
  type: docker-image
  source: 
    repository: gcc
    
inputs:
  - name: worker
  
run:
  path: sh
  args: [-exc]
  args: | 
        cd worker
        gcc -Wall -O2 hola.c -o hola
